//default argument - it is a constant expression assigned to an optional
//parameter appearing at the end of parameter list, it is implicitly passed
//(by compiler) if it is not explictly passed (by caller)
double Capacity(float length, float breadth, float height, float thickness = 1);

