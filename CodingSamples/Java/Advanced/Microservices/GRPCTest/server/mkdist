pack="demo-1.0.0.jar"
mkdir -p dist/target
[ -e target/$pack ] && cp -r target/$pack dist/target
[ -e target/dependency ] && cp -r target/dependency dist/target
cp opt dist
echo 'java @opt -cp target/'$pack':target/dependency/* app.Program $*' > dist/run
chmod +x dist/run

